#!/usr/bin/env bash
# torch-team-init — generate repo-level bootstrap files for team mode
#
# Usage:
#   torch-team-init optional   # gentle CLAUDE.md suggestion, one-time offer
#   torch-team-init required   # CLAUDE.md enforcement + PreToolUse hook
#
# Run from the root of your team's repo (not from the torch directory).

set -euo pipefail

MODE="${1:-}"

if [ "$MODE" != "optional" ] && [ "$MODE" != "required" ]; then
  echo "Usage: torch-team-init {optional|required}" >&2
  echo "" >&2
  echo "  optional  — suggest torch install once per developer (gentle)" >&2
  echo "  required  — enforce torch install, block work without it" >&2
  exit 1
fi

# Must be in a git repo
if ! git rev-parse --show-toplevel >/dev/null 2>&1; then
  echo "Error: not in a git repository. Run from your project root." >&2
  exit 1
fi

REPO_ROOT=$(git rev-parse --show-toplevel)
CLAUDE_MD="$REPO_ROOT/CLAUDE.md"
GENERATED=()

# ── Migrate vendored copy if present ──────────────────────────

if [ -d "$REPO_ROOT/.claude/skills/torch" ] && [ ! -L "$REPO_ROOT/.claude/skills/torch" ]; then
  if [ -f "$REPO_ROOT/.claude/skills/torch/VERSION" ] || [ -d "$REPO_ROOT/.claude/skills/torch/.git" ]; then
    echo "  Found vendored torch copy at $REPO_ROOT/.claude/skills/torch"
    echo "  Team mode uses the global install — removing vendored copy..."
    ( cd "$REPO_ROOT" && git rm -r --cached .claude/skills/torch/ 2>/dev/null ) || true
    if [ -f "$REPO_ROOT/.gitignore" ]; then
      if ! grep -qF '.claude/skills/torch/' "$REPO_ROOT/.gitignore" 2>/dev/null; then
        echo '.claude/skills/torch/' >> "$REPO_ROOT/.gitignore"
      fi
    else
      echo '.claude/skills/torch/' > "$REPO_ROOT/.gitignore"
    fi
    rm -rf "$REPO_ROOT/.claude/skills/torch"
    GENERATED+=(".gitignore")
    echo "  Removed vendored copy and added .claude/skills/torch/ to .gitignore"
  fi
fi

# ── CLAUDE.md snippet ──────────────────────────────────────────

if [ "$MODE" = "optional" ]; then
  SNIPPET='## torch (recommended)

This project uses [torch](https://github.com/TooFaded420/torch) for AI-assisted workflows.
Install it for the best experience:

```bash
git clone --depth 1 https://github.com/TooFaded420/torch.git ~/.claude/skills/torch
cd ~/.claude/skills/torch && ./setup --team
```

Skills like /qa, /ship, /review, /investigate, and /browse become available after install.
Use /browse for all web browsing. Use ~/.claude/skills/torch/... for torch file paths.'
else
  SNIPPET='## torch (REQUIRED — global install)

**Before doing ANY work, verify torch is installed:**

```bash
test -d ~/.claude/skills/torch/bin && echo "TORCH_OK" || echo "TORCH_MISSING"
```

If TORCH_MISSING: STOP. Do not proceed. Tell the user:

> torch is required for all AI-assisted work in this repo.
> Install it:
> ```bash
> git clone --depth 1 https://github.com/TooFaded420/torch.git ~/.claude/skills/torch
> cd ~/.claude/skills/torch && ./setup --team
> ```
> Then restart your AI coding tool.

Do not skip skills, ignore torch errors, or work around missing torch.

Using torch skills: After install, skills like /qa, /ship, /review, /investigate,
and /browse are available. Use /browse for all web browsing.
Use ~/.claude/skills/torch/... for torch file paths (the global path).'
fi

# Check if CLAUDE.md already has a torch section
if [ -f "$CLAUDE_MD" ] && grep -q "## torch" "$CLAUDE_MD" 2>/dev/null; then
  echo "CLAUDE.md already has a torch section. Skipping CLAUDE.md update."
  echo "  To replace it, remove the existing ## torch section and re-run."
else
  if [ -f "$CLAUDE_MD" ]; then
    echo "" >> "$CLAUDE_MD"
  fi
  echo "$SNIPPET" >> "$CLAUDE_MD"
  GENERATED+=("CLAUDE.md")
  echo "  + CLAUDE.md — added torch $MODE section"
fi

# ── Required mode: enforcement hook ────────────────────────────

if [ "$MODE" = "required" ]; then
  HOOKS_DIR="$REPO_ROOT/.claude/hooks"
  SETTINGS="$REPO_ROOT/.claude/settings.json"

  # Create enforcement hook script
  mkdir -p "$HOOKS_DIR"
  cat > "$HOOKS_DIR/check-torch.sh" << 'HOOK_EOF'
#!/bin/bash
# Block skill usage when torch is not installed globally.

if [ ! -d "$HOME/.claude/skills/torch/bin" ]; then
  cat >&2 <<'MSG'
BLOCKED: torch is not installed globally.

torch is required for AI-assisted work in this repo.

Install it:
  git clone --depth 1 https://github.com/TooFaded420/torch.git ~/.claude/skills/torch
  cd ~/.claude/skills/torch && ./setup --team

Then restart your AI coding tool.
MSG
  echo '{"permissionDecision":"deny","message":"torch is required but not installed. See stderr for install instructions."}'
  exit 0
fi

echo '{}'
HOOK_EOF
  chmod +x "$HOOKS_DIR/check-torch.sh"
  GENERATED+=(".claude/hooks/check-torch.sh")
  echo "  + .claude/hooks/check-torch.sh — enforcement hook"

  # Add hook to project-level settings.json
  if command -v bun >/dev/null 2>&1; then
    TORCH_SETTINGS_PATH="$SETTINGS" bun -e "
      const fs = require('fs');
      const settingsPath = process.env.TORCH_SETTINGS_PATH;

      let settings = {};
      try { settings = JSON.parse(fs.readFileSync(settingsPath, 'utf8')); } catch {}

      if (!settings.hooks) settings.hooks = {};
      if (!settings.hooks.PreToolUse) settings.hooks.PreToolUse = [];

      // Dedup
      const exists = settings.hooks.PreToolUse.some(entry =>
        entry.matcher === 'Skill' &&
        entry.hooks && entry.hooks.some(h => h.command && h.command.includes('check-torch'))
      );

      if (!exists) {
        settings.hooks.PreToolUse.push({
          matcher: 'Skill',
          hooks: [{
            type: 'command',
            command: '\"\$CLAUDE_PROJECT_DIR/.claude/hooks/check-torch.sh\"'
          }]
        });
      }

      const tmp = settingsPath + '.tmp';
      fs.writeFileSync(tmp, JSON.stringify(settings, null, 2) + '\n');
      fs.renameSync(tmp, settingsPath);
    " 2>/dev/null
    GENERATED+=(".claude/settings.json")
    echo "  + .claude/settings.json — PreToolUse hook registered"
  else
    echo "  ! bun not found — manually add the PreToolUse hook to .claude/settings.json"
  fi
fi

# ── Summary ────────────────────────────────────────────────────

echo ""
echo "Team mode ($MODE) initialized."
echo ""
if [ ${#GENERATED[@]} -gt 0 ]; then
  echo "Commit the generated files:"
  echo "  git add ${GENERATED[*]}"
  echo "  git commit -m \"chore: require torch for AI-assisted work\""
fi
echo ""
echo "Each developer then runs:"
echo "  git clone --depth 1 https://github.com/TooFaded420/torch.git ~/.claude/skills/torch"
echo "  cd ~/.claude/skills/torch && ./setup --team"
